import cherrypy

class FormSubmission(object):
    @cherrypy.expose
    def index(self):
        return 
    
    @cherrypy.expose
    def formsubmission(self, studentno, firstname, lastname, age, email):
        return 
